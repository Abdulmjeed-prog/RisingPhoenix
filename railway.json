{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd rising_phoenix && python manage.py migrate && python manage.py collectstatic --noinput && python manage.py shell -c \"from django.contrib.auth import get_user_model; User = get_user_model(); username='admin'; password='test@test123'; email='admin@example.com'; u, created = User.objects.get_or_create(username=username, defaults={'email': email, 'is_staff': True, 'is_superuser': True}); created and u.set_password(password); created and u.save()\" && gunicorn rising_phoenix.wsgi:application --bind 0.0.0.0:$PORT"
  }
}